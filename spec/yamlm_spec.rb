# frozen_string_literal: true

RSpec.describe YAMLM do
  it 'has a version number' do
    expect(YAMLM::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
